HF_TOKEN=
WHISPER_MODEL=Systran/faster-distil-whisper-large-v3

# Optional Env Vars
PHRASE_TIMEOUT=
MAX_DURATION=
VAD_THRESHOLD=
GPU_DEVICE=

# Partial transcription settings, can also be changed live via client
PARTIAL_EVERY_N_CHUNKS=3
PARTIAL_MIN_INTERVAL_SEC=0.25
LYRICS_PARTIAL_EVERY_N_CHUNKS=4
LYRICS_PARTIAL_MIN_INTERVAL_SEC=0.35
PARTIAL_AUDIO_MAX_SEC=2.0